apiVersion: v1
kind: Service
metadata:
  name: {{ include "webprotege.fullname" . }}
  labels:
    {{- include "webprotege.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webprotege.selectorLabels" . | nindent 4 }}
